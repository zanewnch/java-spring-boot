package org.zane.enumeration;

// the type of database operation

/*
when invoke, use:
OperationType.INSERT
然後這個variable type is OperationType
 */
public enum OperationType {
    UPDATE,
    INSERT
}
